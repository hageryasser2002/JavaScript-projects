
function throwExp(x, y) {
    if (arguments.length < 2 || arguments.length>2) {
        throw "Enter at least two parameters";
    } else {
    console.log("Function ready");        
    }

}

throwExp(100,20);